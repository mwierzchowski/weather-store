name: Delivery
on: push
#  workflow_run:
#    workflows: [Integration]
#    types: [completed]

jobs:
  Release:
    runs-on: ubuntu-latest
    steps:
      - name: Debug
        run: echo Placeholder job was triggered

  Deploy:
    needs: [release]
    runs-on: ubuntu-latest
    steps:
        - name: Debug
          run: echo Placeholder job was triggered

  Smoke-Test:
    needs: [deploy]
    runs-on: ubuntu-latest
    steps:
      - name: Debug
        run: echo Placeholder job was triggered